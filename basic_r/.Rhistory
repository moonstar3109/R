# 결측치 제거
data %>%
filter(!is.na(score))
# 결측치 제거
data %>%
filter(!is.na(score) & !is.na(gender))
# 결측치 제거
data %>%
filter(!is.na(data))
# 결측치 제거
data %>%
filter(!is.na(data))
# 결측치 제거
data %>%
filter(!is.na(score) & !is.na(gender))
# 결측치 제거
data %>%
filter(!is.na())
# 모든 열에서 결측치가 하나라도 존재한다면 제거 na.omit(data)
# 단 - 분석데이터와 상관없는 열이 NA 값을 가졌을때, 데이터 손실이 발생될수 있으므로, 사용에 주의
na.omit(data)
# 결측치가 포함되 있다면, 결과가 NA로 표기가 됩니다.
mean(data$score)
mean(data$score, na.rm = T)
sum(data$score, na.rm = T)
median(data$score, na.rm= T)
# summarise 요약함수에서 적용
exam <- read.csv("data/excel_exam.csv")
#인덱싱으로 결측치 삽입 exam[행, 열]
exam[c(1,3,5,8,11),"math"] <- NA
exam
table( is.na(exam$math))
#1nd
exam %>%
filter( !is.na(math)) %>%
summarise(sum_math = sum(math),
mean_math = mean(math))
#2nd
exam %>%
filter( !is.na(math)) %>%
summarise(sum_math = sum(math),
mean_math = mean(math))
table( is.na(exam))
mean( exam$math, na.rm = T)
ifelse( is.na(exam$math), T, F )
exam$math <- ifelse( is.na(exam$math), mean( exam$math), na.rm = T), exam$math )
mean( exam$math, na.rm = T)
exam$math <- ifelse( is.na(exam$math), mean( exam$math), na.rm = T), exam$math )
table( is.na(exam)) # 결측치 없음
exam$math <- ifelse( is.na(exam$math), mean(exam$math), na.rm = T) , exam$math )
# ggplot2에 존재하는 mpg데이터를 사용합니다
library(ggplot2)
# mpg데이터를 다음 구문으로 불러와서 결측치를 생성하세요.
mpg <- as.data.frame(mpg)
mpg <- as.data.frame(mpg)
mpg[c(65, 124, 131, 153, 212), "hwy"] <- NA
exam$math <- ifelse( is.na(exam$math), mean(exam$math), na.rm = T ), exam$math )
# Q1
# drv(구동방식) 별 hwy(고속도로연비) 평균이 어떻게 다른지 확인하려고 합니다.
# 결측치를 확인하고 drv, hwy 변수에 결측치가 몇 개 있는지 확인하세요.
mpg %>%
group_by(drv,hwy)
# Q1
# drv(구동방식) 별 hwy(고속도로연비) 평균이 어떻게 다른지 확인하려고 합니다.
# 결측치를 확인하고 drv, hwy 변수에 결측치가 몇 개 있는지 확인하세요.
mpg %>%
group_by(drv)
# Q1
# drv(구동방식) 별 hwy(고속도로연비) 평균이 어떻게 다른지 확인하려고 합니다.
# 결측치를 확인하고 drv, hwy 변수에 결측치가 몇 개 있는지 확인하세요.
mpg %>%
group_by(drv)
# Q1
# drv(구동방식) 별 hwy(고속도로연비) 평균이 어떻게 다른지 확인하려고 합니다.
# 결측치를 확인하고 drv, hwy 변수에 결측치가 몇 개 있는지 확인하세요.
mpg %>%
group_by(drv) %>%
mutate(hwy_avg = mean(hwy) )
# Q1
# drv(구동방식) 별 hwy(고속도로연비) 평균이 어떻게 다른지 확인하려고 합니다.
# 결측치를 확인하고 drv, hwy 변수에 결측치가 몇 개 있는지 확인하세요.
mpg
mpg %>%
group_by(drv) %>%
table(is.na(mpg%hwy))
mpg %>%
group_by(drv) %>%
table(is.na(mpg$hwy))
mpg %>%
group_by(drv, hwy) %>%
table(is.na(mpg$hwy))
mpg %>%
group_by(drv) %>%
table(is.na(hwy))
mpg %>%
group_by(drv)
exam$math <- ifelse( is.na(exam$math), mean(exam$math), na.rm = T )
exam$math <- ifelse( is.na(exam$math), mean(exam$math), na.rm = T ), exam$math ) )
exam$math <- ifelse( is.na(exam$math), mean(exam$math), na.rm = T ), exam$math )
table(is.na(mpg%hwy))
table(is.na(mpg$hwy))
mpg %>%
group_by(drv) %>%
is.na(hwy)
mpg %>%
group_by(drv) %>%
table(is.na(hwy))
mpg %>%
group_by(drv) %>%
table(is.na(mpg$hwy))
table(is.na(mpg %>%
group_by(drv)$hwy))
table(is.na(mpg%hwy)
table(is.na(mpg%hwy) )
table(is.na(mpg$hwy) )
data <- mpg %>%
group_by(drv)
data
data <- as.data.frame(mpg %>%
group_by(drv) )
data
table(is.na(data$drv), is.na(data$hwy))
table(is.na(data$drv))
table(is.na(data$drv), is.na(data$hwy))
data %>%
filter(!is.na(hwy)) %>%
mutate(avg = mean(hwy))
data %>%
filter(!is.na(hwy)) %>%
mutate(avg = mean(hwy)) %>%
arrange(desc(avg))
exam$math <- ifelse( is.na(exam$math), mean(exam$math, na.rm = T ), exam$math )
# Q1
# drv(구동방식) 별 hwy(고속도로연비) 평균이 어떻게 다른지 확인하려고 합니다.
# 결측치를 확인하고 drv, hwy 변수에 결측치가 몇 개 있는지 확인하세요.
table(is.na(mpg))
mpg %>%
filter( !is.na(hwy) )%>%
group_by(drv) %>%
summarise( hwy_mean = mean(hwy)) %>%
arrange(desc(hwy_mean))
d1 <- data.frame(gender = c(1,2,1,1,3),
score = c(12,43,54,63,26) )
d1
table(d1$gender)
# 이상치를 NA값으로 변경
d1$gender <- ifelse(d1$gender == 3, NA, d1$gender)
d1
d1 %>%
filter(!is.na(d1))
summarise(gender_mean = mean(gender )
# NA값을 제거하고 분석
d1 %>%
filter(!is.na(d1)) %>%
group_by(gender) %>%
summarise(gender_mean = mean(gender) )
# NA값을 제거하고 분석
d1 %>%
filter(!is.na(d1)) %>%
group_by(gender) %>%
summarise(gender_mean = mean(gender) )
# NA값을 제거하고 분석
d1 %>%
filter(!is.na(d1)) %>%
group_by(gender) %>%
summarise(gender_mean = mean(score) )
boxplot(mpg$hwy)$stats
boxplot(mpg$hwy)
boxplot(mpg$hwy)$stats
# 극단치 경계가 12, 37이기 때문에 초과, 미만데이터는 NA처리
ifelse(mpg$hwy < 12 | mpg$hwy > 37, NA, mpg$hwy)
# 극단치 경계가 12, 37이기 때문에 초과, 미만데이터는 NA처리
mpg <- ifelse(mpg$hwy < 12 | mpg$hwy > 37, NA, mpg$hwy)
mpg
# 극단치 경계가 12, 37이기 때문에 초과, 미만데이터는 NA처리
mpg$hwy <- ifelse(mpg$hwy < 12 | mpg$hwy > 37, NA, mpg$hwy)
mpg
table(is.na(mpg))
table(is.na(mpg$hwy))
table(is.na(mpg$hwy))
table(is.na(mpg$hwy))
# 극단치 경계가 12, 37이기 때문에 초과, 미만데이터는 NA처리
mpg$hwy <- ifelse(mpg$hwy < 12 | mpg$hwy > 37, NA, mpg$hwy)
table(is.na(mpg$hwy))
# 극단치 경계가 12, 37이기 때문에 초과, 미만데이터는 NA처리
mpg$hwy <- ifelse(mpg$hwy < 12 | mpg$hwy > 37, NA, mpg$hwy)
mpg <- as.data.frame.array(mpg)
mpg <- as.data.frame.array(mpg)
mpg <- as.data.frame.array(mpg)
mpg <- as.data.frame.array(mpg)
mpg <- as.data.frame(mpg)
table(mpg$hwy)
mpg <- as.data.frame(mpg)
mpg
libaray
libaray(ggplot2)
library(ggplot2)
mpg <- as.data.frame(mpg)
mpg
rm(mpg)
library(ggplot2)
mpg <- as.data.frame(mpg)
mpg
table(is.na(mpg$hwy))
rm(mpg)
mpg <- as.data.frame(mpg)
table(is.na(mpg$hwy))
table(mpg$hwy)
table(is.na(mpg$hwy))
# 극단치 경계가 12, 37이기 때문에 초과, 미만데이터는 NA처리
mpg$hwy <- ifelse(mpg$hwy < 12 | mpg$hwy > 37, NA, mpg$hwy)
table(is.na(mpg$hwy))
# NA는 제거후에 분석
mpg %>%
filter( !is.na(hwy)) %>%
group_by(drv) %>%
summarise(mean_der = mean(hwy))
library(ggplot2)
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
mpg <- as.data.frame(ggplot2::mpg)
mpg[ c(10, 14, 58, 93), "drv" ] <- "k"
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
boxplot(mpg$drv)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
boxplot(mpg$drv)$stats
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
boxplot(mpg$drv)$stats
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
mpg
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
mpg$drv <- ifelse(mpg$drv == "k",NA,mpg$drv)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
table(mpg$drv)
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
mpg <- as.data.frame(ggplot2::mpg)
mpg[ c(10, 14, 58, 93), "drv" ] <- "k"
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
table(mpg$drv)
mpg$drv <- ifelse(mpg$drv == "k",NA,mpg$drv)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)stats
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)stats
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)
boxplot(mpg$hwy)
boxplot(mpg$hwy)$stats
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)stats
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)stats
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)$stats
mpg$drv
table(!is.na(mpg$drv))
mpg$cty <- ifelse(mpg$drv < 9 | mpg$drv > 26,NA,mpg$drv)
table(!is.na(mpg$drv))
table(!is.na(mpg$cty))
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)$stats
mpg$cty <- ifelse(mpg$cty < 9 | mpg$cty > 26,NA,mpg$cty)
table(!is.na(mpg$cty))
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
mpg <- as.data.frame(ggplot2::mpg)
mpg[ c(10, 14, 58, 93), "drv" ] <- "k"
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
table(mpg$drv)
mpg$drv <- ifelse(mpg$drv == "k",NA,mpg$drv)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)$stats
mpg$cty <- ifelse(mpg$cty < 9 | mpg$cty > 26,NA,mpg$cty)
# Q3
# drv와 cty의 이상치를 결측처리 했다면, 결측치를 제외한 다음 drv별 cty평균이 어떻게 다른지 확인하세요.
# 파이프라인을 사용합니다. (그룹핑)
table(!is.na(mpg$drv,mpg$cty))
# Q3
# drv와 cty의 이상치를 결측처리 했다면, 결측치를 제외한 다음 drv별 cty평균이 어떻게 다른지 확인하세요.
# 파이프라인을 사용합니다. (그룹핑)
table(!is.na(mpg$drv))
# Q3
# drv와 cty의 이상치를 결측처리 했다면, 결측치를 제외한 다음 drv별 cty평균이 어떻게 다른지 확인하세요.
# 파이프라인을 사용합니다. (그룹핑)
table(!is.na(mpg$drv),!is.na(mpg$cty))
# Q3
# drv와 cty의 이상치를 결측처리 했다면, 결측치를 제외한 다음 drv별 cty평균이 어떻게 다른지 확인하세요.
# 파이프라인을 사용합니다. (그룹핑)
mpg %>%
filter(!is.na(mpg$drv), !is.na(mpg$cty))
# Q3
# drv와 cty의 이상치를 결측처리 했다면, 결측치를 제외한 다음 drv별 cty평균이 어떻게 다른지 확인하세요.
# 파이프라인을 사용합니다. (그룹핑)
mpg %>%
filter(!is.na(mpg$drv), !is.na(mpg$cty))
# Q3
# drv와 cty의 이상치를 결측처리 했다면, 결측치를 제외한 다음 drv별 cty평균이 어떻게 다른지 확인하세요.
# 파이프라인을 사용합니다. (그룹핑)
mpg %>%
filter(!is.na(mpg$drv))
mpg %>%
filter(!is.na(mpg$drv))
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
mpg <- as.data.frame(ggplot2::mpg)
mpg[ c(10, 14, 58, 93), "drv" ] <- "k"
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
table(mpg$drv)
mpg$drv <- ifelse(mpg$drv == "k",NA,mpg$drv)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)$stats
mpg$cty <- ifelse(mpg$cty < 9 | mpg$cty > 26,NA,mpg$cty)
mpg %>%
filter(!is.na(mpg$drv))
mpg%>%
filter( !is.na(drv))
mpg
mpg %>%
filter( !is.na(drv))
rm(mpg)
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
mpg <- as.data.frame(ggplot2::mpg)
mpg[ c(10, 14, 58, 93), "drv" ] <- "k"
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
table(mpg$drv)
mpg$drv <- ifelse(mpg$drv == "k",NA,mpg$drv)
table(mpg$drv)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
boxplot(mpg$cty)$stats
mpg$cty <- ifelse(mpg$cty < 9 | mpg$cty > 26,NA,mpg$cty)
table(mpg$cty)
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
table(mpg$cty)
mpg$cty <- ifelse(mpg$cty < 9 | mpg$cty > 26,NA,mpg$cty)
table(mpg$cty)
mpg %>%
filter( is.na(drv))
mpg
class(mpg)
mpg%>%
group_by(drv)
View(mpg)
mpg
mpg
rm(mpg)
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
mpg <- as.data.frame(ggplot2::mpg)
mpg[ c(10, 14, 58, 93), "drv" ] <- "k"
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
table(mpg$drv)
mpg$drv <- ifelse(mpg$drv == "k",NA,mpg$drv)
table(mpg$drv)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
table(mpg$cty)
boxplot(mpg$cty)$stats
mpg$cty <- ifelse(mpg$cty < 9 | mpg$cty > 26,NA,mpg$cty)
table(mpg$cty)
class(mpg)
mpg
View(mpg)
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
rm(mpg)
mpg
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
rm(mpg)
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
rm(mpg)
mpg
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
rm(mpg)
mpg
mpg <- as.data.frame(ggplot2::mpg)
mpg
mpg[ c(10, 14, 58, 93), "drv" ] <- "k"
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
table(mpg$drv)
mpg$drv <- ifelse(mpg$drv == "k",NA,mpg$drv)
table(mpg$drv)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
table(mpg$cty)
boxplot(mpg$cty)$stats
mpg$cty <- ifelse(mpg$cty < 9 | mpg$cty > 26,NA,mpg$cty)
table(mpg$cty)
mpg
mpg %>%
filter( !is.na(cty))
# Q3
# drv와 cty의 이상치를 결측처리 했다면, 결측치를 제외한 다음 drv별 cty평균이 어떻게 다른지 확인하세요.
# 파이프라인을 사용합니다. (그룹핑)
str(mpg)
mpg
# ==============================================================================================
# 문제
# ggplot2에 존재하는 mpg데이터를 사용합니다
# mpg데이터를 다음 구문으로 불러와서 이상치를 생성하세요.
library(ggplot2)
mpg <- as.data.frame(mpg)
# mpg <- as.data.frame(ggplot2::mpg)
mpg[ c(10, 14, 58, 93), "drv" ] <- "k"
mpg[ c(29, 43, 129, 203), "cty"] <- c(3,4,39,42)
# Q1
# drv에 이상치가 있는지 확인 합니다. 이상치를 결측치로 처리한 다음 확인하세요.
table(mpg$drv)
mpg$drv <- ifelse(mpg$drv == "k",NA,mpg$drv)
table(mpg$drv)
# Q2
# boxplot을 이용해서 cty의 이상치 범위를 확인하고 통계치를 이용해서 벗어난 값을 결측처리 한 후
# 다시 boxplot을 만들어서 확인하세요.
table(mpg$cty)
boxplot(mpg$cty)$stats
mpg$cty <- ifelse(mpg$cty < 9 | mpg$cty > 26,NA,mpg$cty)
# Q3
# drv와 cty의 이상치를 결측처리 했다면, 결측치를 제외한 다음 drv별 cty평균이 어떻게 다른지 확인하세요.
# 파이프라인을 사용합니다. (그룹핑)
str(mpg)
mpg %>%
filter( !is.na(cty))
mpg %>%
filter( is.na(cty))
mpg %>% filter(is.na(cty))
mpg %>% filter(is.na(mpg$cty))
# ==============================================================================================
install.packages("dplyr")
install.packages("dplyr")
mpg %>% filter(is.na(mpg$cty))
library(dplyr)
mpg %>%
group_by(drv)
mpg %>%
filter( !is.na(cty))
mpg %>%
filter( !is.na(cty)) %>%
filter( !is.na(drv)) %>%
group_by(drv) %>%
summarise(mean_cty = mean(cty))
